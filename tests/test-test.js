describe("A test suite", function () {
    beforeEach(function () {});
    afterEach(function () {});
    it('should work', function () {
        for (var i = 0; i < 1000000; i++) {
            assert.isTrue(true);
        }
    });
});
